# Command in the Neato robot's native format
# distance in millimeters to drive Left wheel
float32 left_dist
# distance in millimeters to drive Right wheel
float32 right_dist
# speed in millimeters/second
float32 speed
# Acceleration in millimeters/second (if 0, defaults to "speed")
float32 accel
